package com.axa.sesummit.codinggame.services;

import com.axa.sesummit.codinggame.model.Cover;
import com.axa.sesummit.codinggame.model.QuoteRequest;
import org.junit.Assert;
import org.junit.Ignore;
import org.junit.Test;

import java.time.LocalDate;
import java.util.Arrays;

public class QuoteCalculationServiceTest {

	/**
	 * The input is the same as in the first example here:
	 * https://github.com/arolla/Extreme-Insurance/blob/master/README.md
	 */
	@Test
	@Ignore
	public void calculateQuote() throws Exception {
		// final String country = "DE";
		// final LocalDate departureDate = LocalDate.of(2016, 11, 15);
		// final LocalDate returnDate = LocalDate.of(2016, 12, 15);
		// final Integer[] travellerAges = { 32, 39 };
		// final String[] options = { "MedicalConditions" };
		//
		// final QuoteRequest request = new QuoteRequest(country, departureDate,
		// returnDate, Arrays.asList(travellerAges),
		// Arrays.asList(options), Cover.Basic);
		//
		// final Double result = new
		// QuoteCalculationService().calculateQuote(request);
		// Assert.assertEquals(Double.valueOf(108.0), result);
	}
}